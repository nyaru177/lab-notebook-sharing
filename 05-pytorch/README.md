# 05 PyTorch

对应《实验室新生暑期居家集训学习计划》Week 2 的 PyTorch 基础。

可分享主题（例如，以下内容仅供参考，不限于这些内容）：

- Tensor 创建、索引和广播
- autograd 自动求导
- `nn.Module` 模型定义
- Dataset 与 DataLoader
- 训练循环与评估循环
- MNIST 分类实验

## 提交位置

请在本目录下新建自己的姓名文件夹，再把 Notebook、图片、依赖和说明文件放入其中；不要直接把个人文件放在模块根目录。已提供样例的模块使用 `example/` 文件夹。
